# 妙算
让AI帮我们算一算，下来该怎么做？

# 子项目

``` bash
git subtree add --prefix=services/mss_diting git@github.com:kevincnzhengyang/mss_diting.git main --squash


git subtree pull --prefix=services/mss_diting git@github.com:kevincnzhengyang/mss_diting.git main --squash

git subtree push --prefix=services/mss_diting git@github.com:kevincnzhengyang/mss_diting.git main
```

